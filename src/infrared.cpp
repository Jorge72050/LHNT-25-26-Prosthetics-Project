// code for initialization of the amazon infared pins. 
// #include <Arduino.h>
/* onst int sensorPin = 18; // ESP32 digital GPIO
void setup() {
   pinMode(sensorPin, INPUT);
   Serial.begin(115200);
}

void loop() {
   int state = digitalRead(sensorPin);
   Serial.println(state == HIGH ? "No obstacle" : "Obstacle detected");
   delay(100);
}
   */
